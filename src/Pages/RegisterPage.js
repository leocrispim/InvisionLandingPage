import React, { Component } from "react";
import * as SC from './Styled';

class RegisterPage extends Component {
  render() {
    return (
       <div onClick={this.props.handleSwitchPage}>Test2</div>
    );
  }

}
export default RegisterPage;
